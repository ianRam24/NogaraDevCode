var cuenta = 0;
var topping = 1.9;
var toppingOreo = 1;
var toppingKitkat = 1.5;
var toppingBrownie = 0.75;
var toppingLunetas = 0.95;
var saludo = prompt(
  'Hola bienvenido vas a querer helados, Porfavor responde Si o No'
);

if (saludo == 'SI' || 'si' || 'Si') {
} else {
  alert('salgase de la fila joven');
}
